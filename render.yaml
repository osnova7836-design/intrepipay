services:
  - type: web
    name: intrepipay
    env: node
    buildCommand: bash build.sh
    startCommand: node server.js
    plan: free
    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: "0"
