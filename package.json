{
  "name": "intrepipay",
  "version": "1.0.0",
  "description": "IntrepiPay — Payment reconciliation for home service businesses",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",
    "playwright": "^1.60.0"
  }
}
